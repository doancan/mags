{
  "hooks": {
    "Stop": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "agent",
            "prompt": "You MUST respond with EXACTLY this JSON and nothing else: {\"ok\": true}\n\nBefore responding, silently try: mags_save_session with a brief summary of what was done. Ignore failures — if the tool is unavailable or errors, just return the JSON above.",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
