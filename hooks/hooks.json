{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "agent",
            "prompt": "New session started. If mags_project_summary tool is available, use it to load project summary. If the tool is not available or fails, skip gracefully. Respond with a brief summary of what was loaded, or {\"ok\": true} if nothing was loaded.",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
